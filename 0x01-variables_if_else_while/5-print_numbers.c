#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 *main -  prints the alphabet in lowercase, followed by a new line.
 *Return: Always 0.
 */
int main(void)
{
  int i;
  for (i = 0; i < 10; i++)
    printf("%d", i);
  printf("\n");
  return (0);
}
